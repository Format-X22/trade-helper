export enum EStock {
    BINANCE = 'BINANCE',
    BITMEX = 'BITMEX',
    DERIBIT = 'DERIBIT',
}

export enum ESide {
    LONG = 'LONG',
    SHORT = 'SHORT',
}

export enum EType {
    REVERSE = 'REVERSE',
    TREND = 'TREND',
}

// TODO -
export enum EState {}
